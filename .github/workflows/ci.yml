name: CI/CD basic

on:
  push:
    branches:
      - master

jobs:
  read_hello_file:
    runs-on: ubuntu-latest
    steps:
      - name: Git Clone
        uses: actions/checkout@v2
      
      - name: Read helle.txt
        run: cat hello.txt